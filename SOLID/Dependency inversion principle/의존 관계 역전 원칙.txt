의존 관계 역전 원칙
	상위 모듈은 하위 모듈의 구현 내용에 의존하면 안 된다.
	상위 모듈과 하위 모듈 모두 추상화된 내용에 의존해야 한다.

	의존 관계 역전 원칙을 위반하면 코드의 수정이 힘들어진다.
	(메소드 이름이 바뀌면 모든 메소드의 이름을 바꿔야 한다.)

	상위 모듈 : 클래스가 다른 클래스 사용
	하위 모듈 : 클래스가 사용 당함

	개방-폐쇄 원칙(O) 에서와 같이
	다양한 클래스를 같은 catagory 로 분류해 추상화,
	이후 override 한다.

